This is a small wiki written in Ruby using git as the backend for storing the pages.
Its using Ramaze as the web framework.

Require gems:
ramaze
mojombo-grit (github)
georgi-git_store (github)
rdiscount
